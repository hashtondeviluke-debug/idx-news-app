# ─────────────────────────────────────────────────────────────────────────────
# IDX News Intelligence System — Environment Variables
# ─────────────────────────────────────────────────────────────────────────────
# SETUP:
#   1. Copy this file:  cp .env.example .env
#   2. Fill in your keys below
#   3. NEVER commit .env to git — it's already in .gitignore
# ─────────────────────────────────────────────────────────────────────────────

# ── Gemini API Key (REQUIRED for Step 2) ─────────────────────────────────────
# Get a free key at: https://aistudio.google.com/apikey
# Free tier: 15 req/min, 1,500 req/day, 1M tokens/min — enough for ~1,500 articles/day
GEMINI_API_KEY=AIzaSyCwoQdYVsl9SUEhulOOAMx1kq1wdNyQXA4

# ── Optional: Override default Gemini model ───────────────────────────────────
# Default is gemini-2.0-flash (free, fast).
# Other options: gemini-1.5-pro (higher quality, lower quota)
# GEMINI_MODEL=gemini-2.0-flash

# ── Optional: Proxy settings (if behind a corporate firewall) ─────────────────
# HTTP_PROXY=http://proxy.company.com:8080
# HTTPS_PROXY=http://proxy.company.com:8080
